import styles from "@/styles/overview.module.scss";

export default function Overview() {
  return (
    <section data-scroll-section data-scroll className={styles.container}>
      OVERVIEW
    </section>
  );
}
